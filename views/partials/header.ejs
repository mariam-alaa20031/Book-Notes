<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YourBooks</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
  </head>
<%- include("navbar.ejs")%>
    <div style="margin:0;" class="container-fluid px-5 py-3 section-header">
        <div class="row align-items-center g-lg-5 py-5">
          <div class="col-lg-7 text-center text-lg-start">
            <h1 class="display-4 fw-bold lh-1 text-body-emphasis mb-3">Note Your Books</h1>
            <p class="col-lg-10 fs-4" style="text-align: justify;">Constantly forgetting your favorite reads? This is just the place for you! Find solace in knowing that all the books you've ever read, all your notes, reviews and comments are stored in a safe place. </p>
          </div>
          <div class="col-md-10 mx-auto col-lg-5">
            <form action='<%=locals.login?"/login":"/signup"%>' method="post" class="p-4 p-md-5 rounded-3 " style="border:4px solid rgba(12, 10, 10, 0.747); background-color: #f6f6ee;"  >  
              <% if (locals.error){%>
                <div class="mb-3" style="text-align: center;">
                <small class="mb-4"style="color:rgb(186, 24, 24)"><%=error%></small>
              </div>
             <%}%>
             <%if (locals.success){%>
                <div  style="text-align: center;"class="mb-3">
                <small style="color:green"><%=success%></small>
              </div>
             <%}%>
              <div class="form-floating mb-3">
                <input type="text" class="form-control" required id="floatingInput" name="username" placeholder="Username">
                <label for="floatingInput">Username</label>
              </div>
              <div class="form-floating mb-3">
                <input type="password" class="form-control" required for="password" name="password" id="floatingPassword" placeholder="Password">
                <label for="floatingPassword">Password</label>
              </div>
              <div class="checkbox mb-3">
                <label>
                  <input type="checkbox" value="remember-me"> Remember me
                </label>
              </div>
              <button class="w-100 btn btn-lg btn-primary" type="submit"><%=locals.login?"Sign in":"Sign up"%></button>
              <hr class="my-4">
        
           <%if(!(locals.login)){%>
           <div>
           <small class="text-body-secondary">Already registered? <a href="/login">sign in</a></small></div> 
           <%}%>   
           </form>

          </div>
        </div>
      </div>